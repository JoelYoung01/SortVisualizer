package com.company;

public interface Commons {
    public static int DEFAULT_PERIOD = 500;     //default delay period, used if no other delay available
    public static int DEFAULT_SIZE = 25;        //default array size
    public static int DEFAULT_WIDTH = 500;      //default window width
    public static int DEFAULT_HEIGHT = 500;     //default window height
}

